import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SchoolProyect',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
